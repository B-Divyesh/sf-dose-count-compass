import { readdir, writeFile } from 'node:fs/promises';
const files = (await readdir('dist/assets')).map((name) => `/assets/${name}`);
const precache = ['/', '/index.html', '/404.html', '/404.css', '/hero-diorama.webp', '/manifest.webmanifest', '/favicon.svg', '/icon-180.svg', '/icon-192.svg', '/icon-512.svg', '/social.svg', '/print.css', ...files];
const source = `const C='dose-compass-v6';const P=${JSON.stringify(precache)};const R=['/','/demo','/log','/privacy','/terms'];self.addEventListener('install',e=>e.waitUntil(caches.open(C).then(c=>c.addAll(P)).then(()=>self.skipWaiting())));self.addEventListener('activate',e=>e.waitUntil(Promise.all([caches.keys().then(keys=>Promise.all(keys.filter(key=>key.startsWith('dose-compass-')&&key!==C).map(key=>caches.delete(key)))),self.clients.claim()])));async function missing(){const r=await caches.match('/404.html');if(!r)return new Response('Page not found',{status:404,headers:{'Content-Type':'text/plain'}});return new Response(await r.blob(),{status:404,statusText:'Not Found',headers:r.headers})}self.addEventListener('fetch',e=>{if(e.request.method!=='GET')return;const u=new URL(e.request.url);if(u.origin!==location.origin)return;if(e.request.mode==='navigate'){e.respondWith(R.includes(u.pathname)?caches.match('/index.html').then(r=>r||fetch(e.request)):missing());return}e.respondWith(caches.match(e.request).then(r=>r||fetch(e.request).then(x=>{const copy=x.clone();caches.open(C).then(c=>c.put(e.request,copy));return x})))})`;
await writeFile('dist/sw.js', source);
