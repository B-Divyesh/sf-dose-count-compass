{
  "name": "dose-count-compass",
  "version": "1.2.3",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/write-sw.mjs",
    "preview": "vite preview",
    "serve:test": "node scripts/serve-production.mjs",
    "test": "playwright test",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "1.58.2",
    "axe-core": "^4.10.3",
    "typescript": "^5.7.3",
    "vite": "^6.1.0"
  }
}
